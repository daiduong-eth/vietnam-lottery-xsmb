# API Conventions — xsmb overrides

Rule chung xem `~/.claude/rules/api-conventions.md`. File này chỉ ghi **khác biệt riêng cho xsmb** (VD: endpoint `/api/v1/results/{date}`, format payload kết quả XSMB).

_Chưa có override._
