---
name: deploy
description: Workflow deploy xsmb (project-local override)
---

Override skill `deploy` cho xsmb. Điền chi tiết flow (branch nào → server nào, healthcheck, rollback) khi đã chốt.

Mặc định: dùng skill global `~/.claude/skills/deploy/SKILL.md`.
