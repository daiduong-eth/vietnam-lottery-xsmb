---
description: Deploy xsmb (project-local)
argument-hint: [staging|production]
---

Override deploy flow cho repo xsmb. Điền chi tiết (server target, image tag, healthcheck URL, rollback) khi đã có CI/CD. Mặc định = dùng global `~/.claude/commands/deploy.md`.
