---
description: Review code changes pending trên branch hiện tại của xsmb
---

Override / bổ sung cho `/review` global. Chỉ điền nội dung khi xsmb có flow riêng (VD: phải kiểm tra schema lottery data, format kết quả XSMB, …). Mặc định để rỗng = dùng global `~/.claude/commands/review.md`.
